{
  "name": "ProfessionalNote",
  "type": "object",
  "properties": {
    "family_email": {
      "type": "string"
    },
    "child_name": {
      "type": "string"
    },
    "note": {
      "type": "string"
    },
    "recommendation": {
      "type": "string"
    },
    "professional_name": {
      "type": "string"
    },
    "professional_role": {
      "type": "string"
    },
    "professional_email": {
      "type": "string",
      "description": "Professional who created this note"
    }
  },
  "required": [
    "note"
  ],
  "rls": {
    "create": true,
    "read": {
      "$or": [
        {
          "family_email": "{{user.email}}"
        },
        {
          "professional_email": "{{user.email}}"
        },
        {
          "created_by": "{{user.email}}"
        }
      ]
    },
    "update": {
      "$or": [
        {
          "professional_email": "{{user.email}}"
        },
        {
          "created_by": "{{user.email}}"
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "professional_email": "{{user.email}}"
        },
        {
          "created_by": "{{user.email}}"
        }
      ]
    }
  }
}